<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.androidfinalassignment">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidFinalAssignment">
        <activity android:name=".ShowExerciseContent"></activity>
        <activity android:name=".ShowVocabularyContent" />
        <activity android:name=".ShowGrammarContent" />
        <activity
            android:name=".TabbedHome"
            android:label="@string/title_activity_tabbed_home"
            android:theme="@style/Theme.AndroidFinalAssignment.NoActionBar" />
        <activity android:name=".Home" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>